import type { SwaggerRequestOptions } from './type';
export declare const RequestAPI: (props: SwaggerRequestOptions) => (target: object, key: string | symbol, descriptor: PropertyDescriptor) => PropertyDescriptor;
